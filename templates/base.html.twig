<!DOCTYPE html>
<html>
<head>
    <title>{% block title %}Site de discussion Mickey et TCG{% endblock %}</title>
    {% block stylesheets %}
   <link rel="stylesheet" href="{{ asset('styles/css/header-footer.css') }}?v=1.0">
    {% endblock %}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM" crossorigin="anonymous"></script>
    {% block javascripts %}
        <script src="{{ asset('styles/javascript/header-footer.js') }}" defer></script>
    {% endblock %}
    <link rel="icon" type="image/x-icon" href="{{ asset('images/Favicon.png') }}">
</head>

<header>

<nav id="nav">  
    <a class="logo" href="{{ path('home') }}"><img src="{{ asset('images/Logo.png') }}" alt="Logo du site"></a> 

    <ul>     
        {% if is_granted('ROLE_ADMIN') %}
                <a href=""><li class="admin">Tableau administrateur</li></a>
        {% endif %}
            <a href="{{ path('home') }}"><li>Accueil</li></a>
            {% if app.user %}
            <a href="{{ path('forum') }}"><li>Forum</li></a>
            <a href="{{ path('profil') }}"><li>Mon profil</li></a>
            <a href="{{ path('app_logout') }}"><li>Se déconnecter</li></a>
        {% else %}
            <a href="{{ path('app_login') }}"><li>Se connecter</li></a>
        {% endif %}
    </ul>
    <div id="icon">

    
</nav>

</header>

    <main>
       {% block body %}
        {% endblock %}
    </main>
<footer>
<img src="{{ asset('images/Logo_footer.png') }}" alt="Logo du site pour le pied de page">
    
</footer>
</html>
