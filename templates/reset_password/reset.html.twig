{% extends 'base.html.twig' %}

{% block title %}Réinitialiser votre mot de passe{% endblock %}

{% block stylesheets %}
{{ parent() }}
   <link rel="stylesheet" href="{{ asset('styles/css/form.css') }}?v=1.0">
{% endblock %}

{% block body %}

<div class="mickey">
    <img src="{{ asset('images/mickey-formulaire.png') }}" alt="formulaire en forme de mickey" class="image_mickey">

    <div class='form-mickey'>
    <h1>Réinitialiser votre mot de passe</h1>

    {{ form_start(resetForm) }}
        {{ form_row(resetForm.plainPassword, {
    'label': 'Nouveau mot de passe'|trans
        }) }}
        <button class="bouton2">Mettre à jour le mot de passe</button>
    {{ form_end(resetForm) }}
    </div>
</div>
{% endblock %}
